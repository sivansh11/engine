#ifndef COMPILER_H
#define COMPILER_H

#include "pch.h"

// std::vector<size_t> find_all_occurences_o(std::string &data, std::string toSearch);

void compile(const char *srcPath, const char *outPath);

#endif
